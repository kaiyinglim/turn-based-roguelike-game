package game.enums;

/**
 * A class that represents the enemy's ability to do something to the
 * items and grounds, or players.
 * Currently, it tags itself as an enemy to make sure it really knows its an enemy
 */
public enum EnemyAbility {
    IS_ENEMY,
    IS_UNDEAD,
    IS_INFECTABLE,
    IS_RABID
}
