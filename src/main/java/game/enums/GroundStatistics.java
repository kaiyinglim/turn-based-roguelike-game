package game.enums;

/**
 * GroundStatistics class is an enumeration class designed to categorized  myriad of complex,
 * nuanced statistics an item can possess in the game world.
 * Currently, it tracks a door being locked or not.
 */
public enum GroundStatistics {
    LOCKED,
}
